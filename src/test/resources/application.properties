# suppress inspection "UnusedProperty" for whole file
spring.application.name=desafiomeetime
# Properties HubSpot
config.credentials.client_id=client_id
config.credentials.secret=secret
config.credentials.scope=oauth%20crm.objects.contacts.read
config.credentials.redirect_uri=http://localhost:${wiremock.server.port}/autenticar/auth-callback
config.credentials.grant_type=authorization_code
# Open Feign
config.TokenClient.url=http://localhost:${wiremock.server.port}
config.TokenClient.path=/oauth/v1/token
config.ContactClient.url=http://localhost:${wiremock.server.port}
config.ContactClient.path=/crm/v3/objects/contacts